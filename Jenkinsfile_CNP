#!groovy

@Library("Infrastructure@DTSPO-30107-jenkins-agents-2")

def type = "java"
def product = "opal"
def component = "logging-service-integration-lib"

withPipeline(type, product, component) {
    nonDeployableApp()
    releaseOnMerge()
}